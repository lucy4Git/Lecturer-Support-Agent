# API Design Guidelines

Use versioned REST/streaming contracts, OpenAPI, authenticated tenant context, idempotency keys, pagination, problem-details errors, correlation IDs, optimistic concurrency for pointers, immutable version resources and explicit authorization. Never trust tenant or role claims supplied by clients.
