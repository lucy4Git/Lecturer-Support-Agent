# Temporary External Access

Each grant records sponsor, purpose, assignment, scope, allowed actions, start, expiry, authentication level, data classification and revocation. Downloads, uploads, annotations and exports are separately permissioned. Expiry/revocation is enforced immediately and audited.
