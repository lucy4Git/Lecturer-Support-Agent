# v1.4 Static Validation Evidence

## Completed

| Check | Result |
|---|---|
| Python `compileall` | Passed |
| SQLAlchemy metadata load | 59 tables |
| FastAPI application import | 33 routes |
| Unit tests | 20 passed |
| TypeScript and TSX transpilation syntax | Passed |
| Role catalogue JSON parse | Passed |
| Password credential access contract | Passed |
| Active-role authorisation contract | Passed |
| Active-session enforcement contract | Passed |
| Public logout contract | Passed |
| Restricted authentication-role user-write contract | Passed |
| Release secret-pattern scan | Passed |

## Unit and source-contract coverage

- Argon2 hash and verification.
- Password policy failure cases.
- Access-token role and tenant claims.
- Opaque refresh and invitation token parsing.
- New table registration and tenant ownership.
- Application-role denial of password-credential access.
- Institution Administrator-only position management.
- Active role-assignment restriction in authorisation.
- Protected requests validate the signed session against the tenant-scoped session record.
- Refresh-session revocation does not depend on a still-valid access token.
- `lsa_auth` can create invited users while `lsa_app` remains blocked from global user and password tables.

## Not claimed

No live database, Docker, browser, API, network, email, or provider validation was possible in this checkpoint. These are explicitly pending and must not be reported as passed.
