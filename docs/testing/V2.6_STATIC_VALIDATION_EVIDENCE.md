# v2.6 static validation evidence

## Clean-package validation

The deployment-completion working copy was created without reading or copying the real `.env` file. The following checks passed in the packaging environment:

- Python compilation for `services`, `scripts`, and `tests`;
- 153 unit tests passed;
- 5 Windows-specific event-loop tests were correctly skipped on Linux;
- TypeScript/TSX structural syntax validation passed;
- all cumulative Python release validators through v2.6 passed;
- 39 JSON and 20 YAML files parsed successfully, including multi-document Kubernetes YAML;
- high-confidence repository secret scan passed;
- v2.6 deployment validator confirmed 125 SQLAlchemy tables, migration head `20260803_0012`, protected seeding, access-request routes, deployment descriptors, and migration/parity tools.

## Prior owner-machine evidence retained as baseline

The validated v2.5 owner-machine baseline recorded all eight role previews, 151 unit tests, and 94 integration tests. v2.6 adds deployment and migration code; vendor-runtime validation is intentionally performed after the protected managed services are created.

## Environment limitation

A full `npm ci`/Next.js build could not be repeated in this packaging container because its internal npm mirror did not contain one transitive package. The project lock file remains unchanged and GitHub CI is configured to run a clean Linux `npm ci`, typecheck, and production build after push. The previous owner-machine production build passed.
