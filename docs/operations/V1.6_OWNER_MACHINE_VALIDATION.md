# v1.6 owner-machine validation

1. Copy `.env.example` to `.env` and keep it outside uploaded archives.
2. Start Docker Desktop manually, then run the existing database stack script.
3. Run migrations through revision `20260725_0003` and tenant-isolation tests.
4. Confirm MinIO bucket versioning, Ollama and the configured embedding model, and Qdrant collection dimension 768.
5. Upload TXT, PDF, DOCX, PPTX, XLSX, transcript, video and safe ZIP examples from lecturer, Head of Department and Institution Administrator roles.
6. Verify exact duplicates do not create replacement versions and changed files create sequential versions.
7. Confirm video is transcript-required and image text is not invented.
8. Attach an authorised document to a conversation and verify an institutional source card and retrieval trace.
9. Attempt cross-tenant, cross-department, private-owner and expired-external-reviewer access and confirm denial.
10. Exercise valid and invalid document status transitions; confirm review and approval permissions, transition history, and audit events.
11. Run the Next.js build and live browser preview on desktop, tablet and mobile widths; fix issues before Claude receives the handover.

Run:

```powershell
.\scripts\validation\Validate-V1.6.ps1 -RunOwnerMachineChecks
```
