# v2.1 Owner-Machine Validation

Run from the extracted project root after starting Docker Desktop manually.

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\validation\Invoke-ConsolidatedOwnerValidation.ps1 `
  -Mode full `
  -InstallDependencies `
  -StartInfrastructure `
  -RunLivePreview
```

## Additional v2.1 checks

- Alembic reaches revision `20260725_0007`.
- Search cannot cross institutions or expose another user's private conversation.
- Library and Files exclude inaccessible document versions.
- An attached Library file reaches the next conversation request.
- Saved output remains tied to the version saved after a later edit creates a new version.
- Notification badge, mark-read, and read-all update correctly.
- Institution Administrator cannot see Head of Department assignment actions and vice versa.
- Search, Library, Files, Saved outputs, and Notifications render at desktop, tablet, and mobile breakpoints.
- Dark appearance and `Ctrl/Cmd + K` work.

Status remains **owner-machine validation pending** until the consolidated report passes.
