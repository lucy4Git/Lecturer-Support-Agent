# v2.4 Owner-Machine Validation

After the cumulative infrastructure is running:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\validation\Validate-V2.4.ps1 -RunTests
.\scripts\validation\Test-V2.4DomainAutomationRuntime.ps1
.\scripts\validation\Invoke-ConsolidatedOwnerValidation.ps1 -Mode full -InstallDependencies -StartInfrastructure -RunLivePreview
```

The v2.4 checkpoint is accepted only when schedules, worker execution, external-access expiry, retention preview, reversible retention, reports, audit exports, document ingestion and output export pass against the live services. Backup and restore drills require configured destinations before they may pass.
