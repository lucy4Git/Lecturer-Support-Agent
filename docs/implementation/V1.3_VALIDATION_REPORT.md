# Lecturer Support Agent v1.3 Validation Report

**Checkpoint:** Physical Data and Database Foundation  
**Validation date:** 24 July 2026  
**Result:** PASS for deterministic repository validation

## Completed validation

| Area | Result |
|---|---:|
| Required v1.3 files | 18 present and non-empty |
| SQLAlchemy physical tables | 53 |
| PostgreSQL mock DDL statements | 138 |
| JSON Schemas | 12 valid Draft 2020-12 schemas |
| PlantUML sources | 38 structurally valid |
| Database/security PowerShell scripts | 9 checked |
| Unit tests | 10 passed |
| Multi-provider v1.2 validator | Passed |
| Data Foundation v1.1 validator | Passed |
| Secret-bearing `.env` in repository | Not present |

## Validated controls

- all tenant-owned tables contain `tenant_id` and a foreign key to the institution;
- Institution Administrator and Head of Department permissions remain independent;
- every approved lecturer-support role can receive contextual bulk-upload permission;
- object storage preserves distinct binary versions;
- exact duplicate and changed-content paths are represented separately;
- Qdrant private retrieval always includes the trusted tenant filter;
- source identifiers are deterministic and citation records require a real retrieval relationship;
- PostgreSQL RLS policy and restricted global identity views are included;
- real API keys remain outside the distributable repository; and
- controlled dataset acquisition fails closed until rights approval is explicit.

## Validation not executable in this build environment

The build environment did not provide Docker, PostgreSQL, MinIO, Qdrant, Redis,
PowerShell, or the PlantUML compiler. Therefore, the following owner-machine tests
remain mandatory after extraction:

1. start the local containers manually;
2. run the Alembic migration;
3. seed the two synthetic tenants;
4. execute PostgreSQL RLS integration tests;
5. execute document-version integration tests;
6. render every PlantUML diagram; and
7. exercise PowerShell scripts on Windows.

The repository contains the commands and test files required for those checks.
No production-readiness claim is made until they pass in the target environment.
