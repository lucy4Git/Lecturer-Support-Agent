# Development Workflow

Select requirement IDs; write/update ADR if needed; add failing tests; implement domain and authorization rules; expose versioned API/events and client experience; run quality checks; perform role-based responsive live preview; update docs/traceability; submit PR with evidence and rollback.
