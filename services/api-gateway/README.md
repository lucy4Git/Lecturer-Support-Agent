# API Gateway / BFF

Client aggregation, secure request context, streaming, rate limits, correlation IDs and error translation.

Implementation must follow `PROJECT_CONSTITUTION.md`, requirements, ADRs and security rules.
